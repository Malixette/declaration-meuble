{% extends 'base.html.twig' %} 

{% block main %}
{#{% if idHebergement > 0 %}#}
{#    {{ include('hebergement/_verificationInfo.html.twig') }}#}
{#{% endif %}#}

{{form_start(form)}}
{{form_end(form)}}

{% endblock %}