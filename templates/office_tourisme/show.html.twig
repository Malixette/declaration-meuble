{% extends 'base.html.twig' %}

{% block title %}OfficeTourisme{% endblock %}

{% block body %}
    <h1>OfficeTourisme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ office_tourisme.id }}</td>
            </tr>
            <tr>
                <th>Ot_referent_nom</th>
                <td>{{ office_tourisme.ot_referent_nom }}</td>
            </tr>
            <tr>
                <th>Ot_referent_prénom</th>
                <td>{{ office_tourisme.ot_referent_prénom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('office_tourisme_index') }}">back to list</a>

    <a href="{{ path('office_tourisme_edit', {'id': office_tourisme.id}) }}">edit</a>

    {{ include('office_tourisme/_delete_form.html.twig') }}
{% endblock %}