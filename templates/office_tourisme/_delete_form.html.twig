<form method="post" action="{{ path('office_tourisme_delete', {'id': office_tourisme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ office_tourisme.id) }}">
    <button class="btn">Delete</button>
</form>